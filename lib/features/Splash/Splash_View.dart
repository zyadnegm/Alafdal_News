import 'package:flutter/material.dart';

import 'Widget/SplashView_Body.dart';

class Splash_View extends StatelessWidget {
  const Splash_View({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SplashView_Body(),
    );
  }
}
